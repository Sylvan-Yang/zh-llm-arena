# 🛠 本地运行指南（零基础版）

这个项目已经能跑了。下面每一步照着敲就行，不用理解原理，先跑起来再说。

## 第一步：确认你有 Python

打开「终端」（Mac 上按 Cmd+空格 搜 Terminal），输入：

```bash
python3 --version
```

只要显示 `Python 3.x.x` 就 OK。没有的话去 python.org 下载安装 3.11 或 3.12。

## 第二步：进入项目文件夹

```bash
cd 你把项目放的位置/zh-llm-arena
```

> 小知识：`cd` 是“进入某个文件夹”。把上面路径换成你电脑上 zh-llm-arena 的真实位置。
> 小技巧：在终端输入 `cd `（带空格）后，直接把文件夹拖进终端窗口，路径会自动填好。

## 第三步：装依赖（只做一次）

```bash
pip install -r requirements.txt
```

这会把 `streamlit` 和 `pandas` 两个“零件”装上。第一次会稍微等一会儿。

## 第四步：启动！

```bash
streamlit run app.py
```

跑起来后，终端会显示一个本地网址（通常是 http://localhost:8501 ）。
**按住 Ctrl 键点这个网址**，浏览器就会打开，看到盲测擂台界面。

## 怎么玩 / 怎么验收

1. 左边点「去盲测投票」→ 选类别 → 选题目 → 看两个匿名回答 → 点 A/B/平手。
2. 投几票后，点「看 Elo 排行榜」，就能看到分数随投票变化。
3. 关掉网页：回到终端按 `Ctrl + C`。

## 每个文件是干嘛的（一句话版）

| 文件 | 作用 |
|---|---|
| `app.py` | 网页本体：发题、收票、显示排行榜 |
| `prompts_data.py` | 题库 + 预置的模型回答（你以后往这里加题/加答案） |
| `elo.py` | 算分裁判：把每一票变成 Elo 分数 |
| `votes.csv` | 投票记录（自动生成，别手改） |
| `requirements.txt` | 需要装哪些“零件” |
| `README.md` | 项目说明（给面试官的“门面”） |

## 下一步可以做什么（按性价比排序）

1. **多录几道题的回答**：把 30 道题都去豆包/文心网页版问一遍，把回答填进 `prompts_data.py` 的 `SEED_ANSWERS`。这是最值钱的“原创数据”。
2. **接真实 API 自动生成回答**：用 DeepSeek / 通义 的免费额度，写几行代码自动把 30 题跑完。这样就不用手动复制了。
3. **部署上线**：用 Streamlit Community Cloud（免费），把 GitHub 仓库连上去，就能得到一个公开网址写进简历。
4. **加 LLM-as-Judge**：用强模型当裁判，给每题自动打分（你 README 里写的“可选裁判”）。
5. **上 Supabase**：等真有多人用的时候，再换数据库存票，别一上来就搞。
